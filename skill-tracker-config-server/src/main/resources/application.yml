spring:
  application:
    name: SKILLTRACKER-CONFIG
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ramji013/skilltracker-config-server

server:
  port:
    9107

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost


